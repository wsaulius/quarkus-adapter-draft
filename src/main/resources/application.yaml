quarkus:
  http:
    port: 8080

adapter:
  excel:
    source: orchestrator.xlsx
    routes-sheet: routes
    steps-sheet: steps
    transforms-sheet: transforms
    aggregates-sheet: aggregates
  execution:
    mock-enabled: true
    connect-timeout-ms: 3000
    read-timeout-ms: 5000
