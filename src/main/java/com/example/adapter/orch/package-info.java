/**
 * Downstream step invocation and decorators.
 *
 * <p>This package contains the low-level transport layer for plan steps.
 * A base invoker performs the actual call, while decorators add cross-cutting behavior
 * such as logging and timing.
 *
 * <p>This keeps transport concerns separate from orchestration logic.
 */
package com.example.adapter.orch;
